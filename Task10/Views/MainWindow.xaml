<Window x:Class="Task10.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task10.Views"
        xmlns:converters="clr-namespace:Task10.BusinessLogic"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="350">
    <Window.Resources>
        <converters:BooleanToBrushConverter x:Key="StatusColorConverter" 
                                            TrueBrush="LightGreen" 
                                            FalseBrush="LightCoral"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Логин:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Пароль:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

        <Button Grid.Row="4" Content="Войти" Command="{Binding LoginCommand}" 
                HorizontalAlignment="Center" VerticalAlignment="Top" 
                Width="100" Height="30" Margin="0,0,0,10"/>

        <Border Grid.Row="4" Background="{Binding IsSuccess, Converter={StaticResource StatusColorConverter}}" 
                CornerRadius="5" Padding="10" VerticalAlignment="Bottom">
            <TextBlock Text="{Binding StatusMessage}" 
                       TextWrapping="Wrap" 
                       HorizontalAlignment="Center"
                       FontWeight="Bold"/>
        </Border>
    </Grid>

</Window>
